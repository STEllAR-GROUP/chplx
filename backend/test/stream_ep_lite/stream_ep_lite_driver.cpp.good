// This program file was generated by the chplx compiler.
// The original Chapel program file can be found here: stream_ep_lite.chpl
//
#include <hpx/hpx_init.hpp>
#include <hpx/program_options.hpp>

#include "stream_ep_lite_driver.hpp"

bool chplx_fork_join_executor = false;

hpx::execution::experimental::fork_join_executor* exec = nullptr;
int chplx_fork_join_executor_yield_delay = 100;    //ns
using namespace stream_ep_lite;

int hpx_main(int argc, char* argv[])
{
    stream_ep_lite::__this->__main();
    return hpx::finalize();
}

int main(int argc, char* argv[])
{
    chplx::registerModule<stream_ep_lite::__thisModule>();
    bool help = false;
    hpx::program_options::options_description options;
    options.add_options()("help", hpx::program_options::bool_switch(&help),
        "config var help: bool")("chplx-fork-join-executor",
        hpx::program_options::bool_switch(&::chplx_fork_join_executor),
        "config var chplx_fork_join : bool")(
        "chplx-fork-join-executor-yield-delay",
        hpx::program_options::value(&::chplx_fork_join_executor_yield_delay),
        "config var chplx_fork_join_yield_delay : int")("m",
        hpx::program_options::value(&stream_ep_lite::m),
        "config const m : std::int64_t")("alpha",
        hpx::program_options::value(&stream_ep_lite::alpha),
        "config const alpha : double")("numTrials",
        hpx::program_options::value(&stream_ep_lite::numTrials),
        "config const numTrials : std::int64_t")("epsilon",
        hpx::program_options::value(&stream_ep_lite::epsilon),
        "config const epsilon : double")("printParams",
        hpx::program_options::value(&stream_ep_lite::printParams),
        "config const printParams : bool")("printStats",
        hpx::program_options::value(&stream_ep_lite::printStats),
        "config const printStats : bool")("seed",
        hpx::program_options::value(&stream_ep_lite::seed),
        "config const seed : std::int64_t");
    hpx::init_params init_args;
    init_args.desc_cmdline = options;
    hpx::program_options::variables_map vm;
    auto parser = hpx::program_options::command_line_parser(argc, argv)
                      .options(options)
                      .allow_unregistered()
                      .run();
    hpx::program_options::store(parser, vm);
    hpx::program_options::notify(vm);
    if (help)
    {
        std::cout << options << std::endl;
        return 1;
    }
    return hpx::init(argc, argv, init_args);
}
